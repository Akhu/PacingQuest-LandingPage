---
export const prerender = true
const { title, author, link, description } = Astro.props
---
<div class="flex flex-col">
    <h3 class="text-slate-50 mb-1">{title}</h1>
    <h5 class="text-slate-100"> {author}</h2>
    <p class="text-slate-50">{description}</p>
    <a class="text-slate-50"  href={link}>Acheter</a>
</div>